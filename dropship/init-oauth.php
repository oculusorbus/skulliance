<?php
// Dead since Drop Ship's own OAuth was retired -- was an unfilled stub even
// before that (the placeholder URL was never actually replaced). Kept as a
// redirect rather than deleted outright, in case anything still links here.
header('Location: ../index.php');
exit();