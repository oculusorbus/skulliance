# Bumped (content changed, not just touched -- see version.php's own
# comment for why the content actually has to differ, not just this
# file's mtime on this dev machine) in every commit going forward.
# version.php reads this file's filemtime() as the app's "version"
# signal; header.php's PWA update-banner JS polls version.php and
# compares it against what was baked into the currently-loaded page,
# showing a refresh prompt (or silently reloading) on a mismatch. Only
# the mtime matters -- nothing parses this content -- but a timestamp
# doubles as a human-readable deploy log via `git log -p VERSION`.
2026-09-03T16:24:27Z
